#include "holberton.h"

/**
 * times_table - print out the 9 times table
 *
 * Return: void
 */
void times_table(void)
{
	int v,h;

	for (h = 0 ; h < 10 ; h++)
	{
		for (v = 0 ; v < 10 ; v++)
		{
			char *sum = (char) (v * h);
			_putchar(sum[0]);
			_putchar(sum[1]);
		}
	}
}
